export const updateShopType = (type) => (
    {
        type: 'UPDATE-TYPE',
        payload: type
    }
)

export const updateItemList = (itemList) => (
    {
        type: 'UPDATE-ITEMLIST',
        payload: itemList
    }
)