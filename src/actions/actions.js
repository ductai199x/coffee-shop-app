export const updateShop = (shopContent) => (
    {
        type: 'UPDATE-SHOP',
        payload: shopContent
    }
)

export const updateItemList = (itemList) => (
    {
        type: 'UPDATE-ITEMLIST',
        payload: itemList
    }
)